#include <stdio.h>
#include <string.h>

int main(void) {
//     const char* lineConst = "GET /user-agent HTTP/1.1\r\nHost: localhost:4221\r\nUser-Agent: strawberry/strawberry-apple\r\n\r\n"; // The input string
//     char line[256]; // Copy of the input
//     char* subString; // Result

//     strcpy(line, lineConst);
//     char delim[] = "\r\n";
// char* token;
// for (token = strtok(line, delim); token; token = strtok(NULL, delim)) {
//   printf("token=%s\n", token);
// }


strstr()
}